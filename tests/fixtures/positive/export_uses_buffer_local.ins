To sum local buffer, giving i32, exported as ins_sum_local_buffer.
Let cells be buffer of 4 i32 filled with 5.
Let total be 0.
For each index i of cells: total becomes total plus cells at i.
Give total.

To main, giving i32.
Give sum local buffer.
