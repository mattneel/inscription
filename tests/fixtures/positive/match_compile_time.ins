Enum Mode backed by u8 has idle be 0; active be 1.

Constant mode: Mode be Mode.active.

Constant code: i32 be match mode:
Mode.idle gives 0;
Mode.active gives 7;
otherwise gives 1.

Check code is equal to 7.

To main, giving i32.
Give code.
