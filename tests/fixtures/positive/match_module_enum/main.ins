import Protocol

code for mode mode: Protocol.Mode gives i32:
  match mode:
    Protocol.Mode.idle gives 0
    Protocol.Mode.active gives 7
    otherwise gives 1

main gives i32:
  code for mode Protocol.Mode.active
