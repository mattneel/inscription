Import Protocol.
To code for mode mode: Protocol.Mode, giving i32.
Give match mode:
Protocol.Mode.idle gives 0;
Protocol.Mode.active gives 7;
otherwise gives 1.

To main, giving i32.
Give code for mode Protocol.Mode.active.
