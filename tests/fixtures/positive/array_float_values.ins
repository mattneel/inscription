sum float view cells: view of f64 gives f64:
  let total: f64 be 0.0
  for each index i of cells:
    total becomes total plus cells at i
  total

main gives i32:
  let weights be array of 3 f64 containing 0.25, 0.5, 1.25
  sum float view weights as i32
