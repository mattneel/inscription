branch owned flag: i1 gives i32:
  let result be 0
  if flag:
    let cells be owned buffer of 4 i32 filled with 0
    cells at 0 becomes 7
    result becomes cells at 0
  otherwise:
    result becomes 3
  result

main gives i32:
  branch owned true
