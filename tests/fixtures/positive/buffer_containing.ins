buffer containing sum gives i32:
  let cells be buffer of 4 i32 containing 1, 2, 3, 4
  let total be 0
  for each index i of cells:
    total becomes total plus cells at i
  total

main gives i32:
  buffer containing sum
