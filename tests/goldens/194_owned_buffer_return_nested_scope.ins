make cells count: i32 gives owned buffer of i32:
  let cells be owned buffer of count i32 filled with 2
  cells

nested caller flag: i1 gives i32:
  let result be 0
  if flag:
    let cells be make cells 4
    result becomes length of cells
  otherwise:
    result becomes 1
  result

main gives i32:
  nested caller true
