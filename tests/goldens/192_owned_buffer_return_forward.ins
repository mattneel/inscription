make ones count: i32 gives owned buffer of i32:
  let cells be owned buffer of count i32 filled with 1
  cells

forward ones count: i32 gives owned buffer of i32:
  make ones count

main gives i32:
  let cells be forward ones 5
  length of cells
