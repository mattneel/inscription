absolute using branch of n: i32 gives i32:
  track result: i32 from 0
  if n is less than zero:
    result becomes zero minus n
  otherwise:
    result becomes n
  result

main gives i32:
  absolute using branch of (zero minus 7)
