module modules.Tokens

union Token:
  eof
  operator symbol: u8 and precedence: u8

score token token: Token gives i32:
  match token:
    Token.operator with symbol as op and precedence as prec gives (op as i32) plus (prec as i32)
    otherwise gives 0
