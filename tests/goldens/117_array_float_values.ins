To sum float view cells: view of f64, giving f64.
Let total: f64 be 0.0.
For each index i of cells: total becomes total plus cells at i.
Give total.

To main, giving i32.
Let weights be array of 3 f64 containing 0.25, 0.5, 1.25.
Give sum float view weights as i32.
