Enum Mode backed by u8 has idle be 0; active be 1.

To guarded mode mode: Mode and enabled: i1, giving i32.
Give match mode:
Mode.active when enabled gives 7;
Mode.active gives 3;
Mode.idle gives 0.

To main, giving i32.
Give guarded mode Mode.active and true.
