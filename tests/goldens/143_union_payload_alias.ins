Union Token has eof; operator symbol: u8 and precedence: u8.

To main, giving i32.
Let symbol be 1.
Let token be Token.operator with symbol be 10 and precedence be 5.
Give match token:
Token.operator with symbol as op and precedence as prec gives symbol plus (op as i32) plus (prec as i32);
otherwise gives symbol.
