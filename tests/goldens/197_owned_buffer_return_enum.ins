enum Mode: u8:
  idle be 0
  active be 1

make modes gives owned buffer of Mode:
  let modes be owned buffer of 3 Mode filled with Mode.idle
  modes at 1 becomes Mode.active
  modes

main gives i32:
  let modes be make modes
  7 when modes at 1 is equal to Mode.active
  otherwise 3
