checked loop owned view start: i32 and count: i32 gives i32:
  let result be 0
  for i from 0 up to 1:
    let cells be owned buffer of 4 i32 filled with 0
    cells at 1 becomes 7
    let window be view of cells from start for count
    result becomes window at 0
  result

main gives i32:
  checked loop owned view 1 and 2
