# Grammar

The current grammar mirror is `grammar/inscription-v0.64.ebnf`. The historical grammar mirrors remain in `grammar/`.

Start there for exact punctuation productions. The book explains the grammar narratively but does not replace the versioned grammar files as compatibility artifacts.
