# Arrays

Arrays are immutable fixed-size local storage.

```inscription,check
To main, giving i32.
Let values be array of 4 i32 containing 1, 2, 3, 4.
Give length of values.
```

Arrays can be read, iterated, and borrowed as read-only views. They cannot be mutated or used as phrase parameters directly.
